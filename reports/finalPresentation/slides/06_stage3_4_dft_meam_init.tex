% =====================================================================
% Slide 9: Stage 3-4 -- how DFT reference data initialises the MEAM potential
%   Top    : a 3-step conceptual flow (references -> rules -> library)
%   Bottom : single-element initialisation (alpha) | cross-terms (heatmap)
% The full 10-row elemental table lives in the report; here we keep only
% the scope summary so the slide reads as structure, not a data dump.
% =====================================================================
\begin{frame}{Stage 3--4: DFT References $\to$ MEAM Initialisation}
\fwtag{Quantum Espresso}

% ---------- top: conceptual flow band ----------
\centering
\begin{tikzpicture}[
    node distance=0.7cm,
    box/.style={rectangle, rounded corners, draw=darkred, fill=darkred!10,
                text width=3.0cm, minimum height=1.0cm, align=center,
                font=\small\bfseries},
    arr/.style={-{Stealth[length=2.5mm]}, thick, darkred},
    lab/.style={font=\scriptsize, text=darkred!70!black, midway, above},
  ]
  \node[box] (ref) {DFT references\\{\scriptsize + existing MEAM-file\\cross-interactions}};
  \node[box, right=2.0cm of ref] (init) {MEAM initialisation\\{\scriptsize $\alpha$ + mixing sign}};
  \node[box, right=2.0cm of init] (lib) {12-element\\MEAM library};
  \draw[arr] (ref) -- (init);
  \draw[arr] (init) -- (lib);
\end{tikzpicture}

\vspace{0.6em}

% ---------- bottom: the two MEAM term families ----------
\begin{columns}[T]
\column{0.46\textwidth}
\textcolor{darkred}{\textbf{Single-element terms}}
\begin{block}{}
\scriptsize
Each element's $(a_{0}, E_{\mathrm{coh}}, B)$ pins the universal binding
curve (spacing, well depth, curvature) and fixes the MEAM decay
exponent
\[
  \alpha = \sqrt{9 B \Omega / E_{\mathrm{coh}}}\qquad(\Omega:\text{atomic volume}).
\]
\end{block}
\scriptsize
\textbf{Scope.} 12 elements (FCC/BCC/HCP/diamond);
$\alpha\!\approx\!4.5$ (Al,\,Si) to $10.9$ (Zn).
PBE--GGA plane waves (Quantum Espresso); $a_0,B$ from an
equation-of-state fit.

\column{0.54\textwidth}
\centering
\textcolor{darkred}{\textbf{Cross-terms}}\\[0.3em]
\includegraphics[height=3.6cm,keepaspectratio]{figures/formation_energy_heatmap.png}\\[-0.1em]
\scriptsize Binary formation energies $E_{\mathrm{form}}(i,j)$ (eV/atom):
negative $=$ exothermic mixing, positive $=$ limited solubility;
the sign seeds each cross-term.
\end{columns}
\end{frame}
